#include "main.h"

/**
 * print_alphabet - print alphabet in small case
 *
 * Return: empty
 */
void print_alphabet(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'z'; ch++)
		_putchar(ch);
	_putchar('\n');
}

